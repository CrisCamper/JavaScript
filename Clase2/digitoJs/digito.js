const digitVericator = function(){

    const digitInput = document.getElementById("digit");

    // get digit value
    const digit = parseInt(digitInput.value);

    // convert number to string
    modifiedDigit = toString(digit);

    // !invert the string
    invertDigit = parseInt(modifiedDigit.split('').reverse().join(''));

    // !multiply digits
    for ()
}